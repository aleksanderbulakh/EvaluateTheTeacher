@model IEnumerable<AvaluateTheTeacher1.Models.Teachers.Teacher>
@{
    ViewBag.Title = "ListOfTeachers";
}

<h2 class="title">Список викладачів</h2>
@foreach (var T in Model)
{
    <div class="teachers">
        <img src="~/TeacherImg/@T.PathToPhoto" alt="Нет фотографии">
        <div class="about_teacher">
            <span>@T.LastName</span>
            <span>@T.Name</span>
            <span>@T.SurName</span>
            <p>@T.Description</p>
            @foreach (var t in T.Ratings)
            {
                <span>@t.AvgRating</span>
                <span>@t.AvgInterest</span>
                <span>@t.AvgQuality</span>
                <span>@t.AvgRelevantToStudents</span>
            }
        </div>
        <a class="vote-teacher" href="/Votings/Votings/@T.TeacherId">Проголосувати за викладача</a>
    </div>
}